# CLAUDE.md — このリポジトリでの作業規約

Claude Code（および人間の共同作業者）向けの、Yajir リポジトリでの取り決め。

## ドキュメント（.md ファイル）

- **原則、日本語で書く**。README・`docs/`・`change_history.md`・本ファイル等すべて。
  （コミットメッセージは英語運用、コード内コメントは各ファイルの既存の流儀に合わせる。）

## 変更履歴の二層構成

版を切るときの履歴の置き場所を、用途で2つに分ける:

- **`change_history.md`**（リポジトリ直下・**push 対象**）＝ **全変更履歴**（最新が上）。
  版を切るたびに、ここへ新エントリを追記する。公開する正史はこちら。
- **`README.md` の「変更履歴」欄**＝ **直近の更新分“だけ”**（全部は載せない）。
  push の際に**最新版1件へ更新**し、それ以前は `change_history.md` へのリンクで誘導する。
  README はトップに来るので「今なにが最新か」だけを見せ、肥大化させない。

## 仕様書メモ

- 仕様本体 `script_spec_v*.md` は**開発用ローカルメモ**で **push しない**（`.gitignore` 済み）。
  版ごとにファイルを分けて手元に残す運用。公開する履歴は上記 `change_history.md` に切り出してある。
- `build.bat` もローカル運用（push しない）。

## push

- **push は毎回、明示の指示があってから**行う（勝手に push しない）。
- `src/core/` を変更したら、VS2022 の `.vcxproj` と STM32/Pico 側ビルドの再同期が必要（人間側で対応）。
