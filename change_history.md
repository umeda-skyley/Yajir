# Yajir 変更履歴（change history）

> Yajir の版ごとの変更点。**最新が上**。言語仕様の本体（設計思想・ポート・変数スロット・文法・ホストAPI 等）は
> 別ファイル `script_spec_v*.md`（開発用ローカルメモ・非push）にあり、本ファイルはその**変更履歴だけを切り出した**もの。
> リポジトリに含める公開履歴はこちら。

### 変更履歴（v0.4.8 → v0.4.9）

**現場要望まとめ**——実機を触る中で出た小粒な使い勝手要望を3点。(1)(2) は純コンパイル時糖衣（**新opcodeゼロ・バイトコード不変・実行時コストゼロ・追加RAMゼロ**）、(3) は文字列ユーティリティ・ポート1本の追加。

**(1) 行継続 `\`（バックスラッシュ＋改行）**——長い文・長い構造化文字定数を物理行に折り返して書けるようにする。

- ルールは1つ: **`\` の直後が改行なら行継続**。バックスラッシュ・改行（CRLF/LFCR は1改行に畳む）・**次行の行頭水平空白（スペース/タブ）**をまとめて消す（行番号は進めるのでエラー行は正確）。文脈で効き方が変わる: **文字列の外**＝論理行をつなぐ（`T_NEWLINE` を出さない。`def_alias(NAME, \`〈改行〉、`A, B, \`〈改行〉、`->` 直前で切る、等の折り返しが可能）／**文字列の中**＝文字列を継続し**行頭インデントを値に含めない**（整形の字下げが値を汚さない）。
- 意味論: 行頭空白は**捨てる**（単一行版と厳密に同値）。意図的な空白を折り返し位置に残すには `\` の“前”に置く（前行末の空白は保持）。裸の改行（`\` なし）は従来どおりエラー（文字列は明示継続しない限り1行）。
- 後方互換: 意味を与える対象——文字列内 `\`+改行（旧 `bad escape sequence`）／文字列外の裸 `\`（旧 `unexpected character`）——はどちらも現状エラーになる並びなので、有効な既存スクリプトは1つも挙動が変わらない。
- 実装: `tokenizer.c` のみ2箇所（`lex_string` と `lex_next` の空白スキップ）。詳細は仕様書本体 §2。

**(2) ビルド設定定数 `CFG_*` をスクリプトから参照可能に**——スロット数や文字列長をプラットフォーム非依存で書けるようにする。

- **`CFG_*` を式の値位置で int リテラルへ展開する**コンパイル時定数。値は「そのビルドの `script_config.h` の実値」なので、`CFG_VAR_COUNT -> REPEAT … ITR -> VAR … END` のように**スロットを過不足なくなめる**コードが移植先の設定に自動追従する（`1..CFG_VAR_COUNT` がちょうど `VAR[0..COUNT-1]`）。
- **ポートも alias スロットも消費しない**（`def_const`＝ポート消費／`def_alias`＝alias 表消費、のどちらとも別枠。使わない人のコストは完全ゼロ）。**読み取り専用**（送り先には立てられない）・**予約名**（`def_alias` 等で上書き不可）。
- 公開範囲は移植に必要な**「寸法」定数14個に限定**——スロット数（`CFG_GVAR_COUNT`/`CFG_VAR_COUNT`/`CFG_ARG_COUNT`/`CFG_SGVAR_COUNT`/`CFG_SVAR_COUNT`/`CFG_SARG_COUNT`）・文字列長（`CFG_SSTR_LEN`/`CFG_SARG_LEN`）・資源スロット数（`CFG_TIMER_SLOTS`/`CFG_DELAY_SLOTS`/`CFG_CALL_NEST`/`CFG_LOOP_NEST`/`CFG_EVENT_QUEUE_LEN`/`CFG_MAX_PORTS`）。内部実装限界（`CFG_CODE_SIZE`/`CFG_INSTR_BUDGET` 等）は非公開＝スクリプトが内部に依存しないよう隠す（非公開名は通常の未知名エラー）。
- 実装: `compiler.c` に定数テーブル1本＋`parse_primary`/`is_reserved_name` の数行のみ。詳細は仕様書本体 §3。

**(3) 文字列ユーティリティ `ASC`（文字→数値）を追加**——`"A" -> ASC` → `65`。

- **文字列の先頭1バイトを 0..255 で返す。空文字列は 0**（`RESULT` 産出）。`CHR`（int→1文字str）の**対**＝文字から数値への一発変換で、これまで欠けていた向き。Yajir では `'A'` は既に int 65（見た目だけ）なので、`ASC` の入力は**文字列が自然**（`"A" -> ASC` == 65／`CHR` と往復: `90 -> CHR -> ASC` == 90）。
- ポート引数は緩い（静的型チェックしない）ので、int を送っても実行時に空文字列扱いで 0（＝非文字列は 0）。スロット経由（`SGVAR[0] -> ASC`）でも先頭バイトを読む。
- 実装: `strutil.c` に thunk 1本＋`register_strutils` に1行（`SCRIPT_T_INT` 産出）。did-you-mean 候補にも `ASC`/`CHR` を追加。詳細は仕様書本体 §3。

### 変更履歴（v0.4.7 → v0.4.8）

**在庫整理 + `NOW`/`STDOUT` のコア昇格**——機能追加ではなく、**ホスト境界（§11）の引き直し**。ポートを「誰の持ち物か」で仕分け、コアが必要とする／コアの知識が漏れている2つをホストのユーザ定義領域からコアへ引き上げる。

**(1) ホスト在庫の整理**——大昔に PC ホストにだけ残っていた未使用ポート/定数（`HOT` `SYS_INIT` `I2C1` `VMSIZE` `VAL_CALCULATOR` `BUZZER`）と、`RXDATA`／`SW1` を削除。**PC と STM32 の登録ポートを一致**させ、`scripts/` のサンプルが「PC で試したものがそのまま実機で動く」状態にした。差分は「C側から自作イベントを上げる実例」枠の `MYHANDLER`（PCは擬似発火まで、実機は登録のみ＋雛形コメント）だけ。判定基準は「よく使うか」ではなく **「コアがそれを必要としているか／コアの知識が漏れているか」**。この基準で `DELAY` は自動的にホスト側に残る（コアはブロッキング遅延を必要としない・協調モデルにむしろ反する意地悪専用ツール）。

**(2) `NOW` のコア昇格**（§3 予約ポート, §8, §11）——動機は「よく使うから」ではなく「**コアの内部クロックがユーザ定義領域を名前で拾いに行っていたから**」。v0.4.7 まで `vm_now()` は毎tickポート表を線形走査して `PK_IN "NOW"` を `strcmp` で探し、無ければ黙って `0` を返していた（＝タイマ/周期/`WAIT`/遅延post が全部これに乗るのにサイレント故障）。`script_vm_t` にクロック関数ポインタ `now_fn` を持たせ、専用API `script_register_now()` で束ねて **O(1)** 化。**未登録なら script_load 時に新ロードエラー `ERR_NO_CLOCK`** で fail-fast（移植時の「原因不明で時間が止まる」を潰す）。旧 `script_register_in("NOW",…)` でも初回走査で `now_fn` にキャッシュして通す（後方互換）。

**(3) `STDOUT` のコア昇格**（§3 予約ポート, §11）——動機は「重複」。PC と STM32 の `th_stdout` は違うのがシンク（`fputs`/`yajir_puts`）だけで、整形ポリシー（argc ループ／`script_val_is_str` タグ判定／int→10進／末尾 `\r\n`）は完全同一。しかもタグ判定はコアの概念（`SV_CHAR` 廃止の v0.4.2 で両ホストを直して回った＝漏れの証拠）。ポリシーをコアが所有し、ホストは `script_register_stdout(puts_fn)` で**シンクだけ**渡す。STM32 の手書き `int_to_str()` が丸ごと消える。`STDOUT` は**任意**（コアは内部で使わない・未登録なら `-> STDOUT` はコンパイル時 `ERR_UNKNOWN_PORT`）——`NOW`（必須）との非対称が「コア依存 vs コア所有ポリシー」の違いを表す。

> **依存の向きは変えない。** `extern yj_host_tick()` のようにコア→ホストの依存を作ると §11 の向き（ホスト→コア）が反転する。そうではなく**登録APIをコアが握る**。コアが知るのは「クロック関数ポインタ」と「シンク関数ポインタ」という抽象だけで、相変わらずプラットフォームを知らない。RAM 実質据え置き（ポインタ2本）、毎tickの `strcmp` 全走査が消える分むしろ軽くなる。

### 変更履歴（v0.4.6 → v0.4.7）

**「時間軸」2点セット**——組み込みの本体は結局「時間をどう扱うか」であり、Yajir に足りなかった時間軸の道具を2つ揃える: **(1) 遅延post `AFTER`**（イベントを未来へ送る）と **(2) 条件トリガ `ON <周期> (条件)`**（状態の変化からイベントを作る）。どちらも新opcodeは0〜1個、RAM微増。

> 設計の背景: モールス送信のような時系列制御を「MAIN の中で `WAIT` を刻んで」書くのが最もやってほしくない筋。`WAIT` はシステムを止めこそしないが、**MAIN は1本しかない**ので、その時間軸を1つの処理が独占してしまう（LEDとブザーを別パターンで同時に、が破綻する）。しかも `WAIT` はハンドラで使えないのでイベント駆動と接続しない。**MAIN+WAIT＝タイムライン1本／遅延post＝タイムラインN本**。この2つは「時間軸を増やす／時間軸を生む」ための道具である。

**(2) 条件トリガ `ON <周期> (条件)`＝エッジ**（§6, §13）。§16 で「決定に必要なのは実使用感のみ」として保留していた項目を、実機の使用感（ADC しきい値・GPIO パルス検出）を得て**エッジで確定**。偽→真の瞬間に1回だけ発火し、偽で自動リセット、再成立で再発火。グローバルのフラグを1個も使わずに書け、可読性も上がる。旧 §16 の保留点だった「フラグ系条件のクリア作法」は、**エッジ採用により意味論に内包されて解決**（再発火には条件を一度落とす＝再武装が必要、というのがユーザー責務で正しい）。実装は**新opcode ゼロ**・`block_t` に `cond_start`+`prev` で **+64B**。詳細は §6。

**(1) 遅延post `値リスト -> ハンドラ AFTER <ms>`（引数を持ったままハンドラを N ms 後に起動）**（§10, §12, §13）。「あとで起きる」手段の表にあった**唯一の穴**を塞ぐ。

| 手段 | 遅延 | 引数 | 宛先 |
|---|:--:|:--:|---|
| `値リスト -> ハンドラ`（post） | ✗ 即時 | ✓ | 任意 |
| `ON <周期>` | ✓ 周期 | ✗ | 固定ブロック |
| `<ms> -> TIMER` | ✓ | **✗** | `ON TIMER` 1個だけ |
| `<ms> -> WAIT` | ✓ | — | MAIN/INIT のみ |
| **`値リスト -> ハンドラ AFTER <ms>`（v0.4.7）** | **✓** | **✓** | **任意** |

- **動機**: 実機（Pico 2 W のモールス送信）で、`TIMER` が引数も宛先も持てないため、遅延を**イベントキュー越しのビジーspin**（毎tick 自分を post し直して `NOW` をポーリング）で再発明せざるを得なかった。「N ms 後にこの状態でもう一度呼んで」が書けないことが根本原因。
- **なぜポートでなく構文か**（設計判断の記録）: 当初 `INVOKER` の遅延版ポート（`"NAME", ms, args… -> DELAY_INVOKER`）を検討したが、**ポートの入力は `argv` しかない**ため宛先も遅延も**ARG/SARG に詰める**しかなく、`CFG_ARG_COUNT=4` の**半分を配管が食う**。さらにハンドラ名は str なので **SARG を1枠消費＝`SARG_LEN × (1+CALL_NEST+QUEUE_LEN)` ＝ 672B（`SARG_LEN=32`）〜2.7KB（同128）**という桁違いのコストになる。`AFTER` なら**宛先はバイトコード1バイト（portidx）・遅延はスタック**で、**ARG/SARG を1枠も使わない**＋ハンドラ名のコンパイル時チェックも効く。よって1段上げて構文にする。
- **意味**: `expr_list` を評価して payload に、`AFTER` の式（int）を遅延msとして、`due = now + ms` で pending 表に積む。満期時に**通常のイベントキューへ enqueue**（＝以後は普通の post と完全に同じ経路・`ON <名前>` が走る）。**文字列引数は post 時にコピー**（満期までに元 `SVAR` が変わっても安全）。
- **`AFTER` はハンドラ源宛のみ**（他の的に付けたら `ERR_BAD_POSITION`）。**`AFTER <=0` は即post に縮退**（エラーにしない・正常な退化）。**キャンセルは無い**（`TIMER` と同様・最小に保つ）。同tickに複数満期したときの相互順序は未規定（§16 の「同tick混在イベントの順序」に合流）。
- **INIT フェーズは `TIMER` の作法**（v0.3.4 の `init_armed` と同じ）: INIT中に張った遅延postは**破棄せず t0 起点で解決**する。理由＝橋を閉じる目的は「**外来**イベントが凍結中に溜まって t0 でスタンピードするのを防ぐ」ことであり、遅延postは**スクリプト自身が書いた予約**だから。これにより **`INIT` からスクリプト定義ハンドラの起動を予約できる**（従来は post が破棄され `TIMER` は引数/宛先を持てないため書けなかった穴）:
  ```
  INIT
      … 初期化 …
      none -> KICKOFF AFTER 3000     // t0 から3秒後に ON KICKOFF
  END
  ```
- **資源**: pending 表 `CFG_DELAY_SLOTS`（既定 **4**）。1枠 ≈ `event_t + due + init_armed` ≈ **180B**（既定構成）→ +約720B。※`SARG_LEN=128` の実機構成では event_t が ~556B に膨らみ 4枠で **+2.2KB** になるので、実機は 2 枠へ絞るのが現実的（`SARG_COUNT × SARG_LEN` の乗数が効く箱がまた1つ増えた形・§12）。
- **満杯**: post 時に pending が満杯なら**新着を捨てて新STATUSビット `ERR_DELAY_FULL`(0x20)** を立てる（資源ごとに専用ビット、という既存の作法に一致）。失敗点は2箇所で切り分け可能——**post時の pending 満杯＝`ERR_DELAY_FULL`／満期時のキュー満杯＝既存 `ERR_QUEUE_OVF`**。
- 実装インパクト: 新opcode **`OP_POST_HANDLER_AFTER`**〈portidx, argc〉1個（遅延はスタック top から pop）＋文法1行（`send` に `[ "AFTER" expr ]`）＋vm に pending 表＋`sched_tick` に満期スキャン＋新定数 `CFG_DELAY_SLOTS`＋新STATUSビット `ERR_DELAY_FULL`。**遅延post は概念上 `TIMER` を包含する**（`TIMER`＝引数なし・宛先固定の遅延post）が、`TIMER` は既存互換のため据え置き。

### 変更履歴（v0.4.5 → v0.4.6）

**ブロック局所エイリアス `def_local`（ハンドラ/ポート/INIT/MAIN の冒頭で `VAR`/`SVAR`/`ARG`/`SARG` に名前を付ける）**（§3, §4, §13）。ハンドラやスクリプト内ポートが複雑になると `VAR[3]` / `SARG[2]` が何を指すか読めなくなる（セルオートマトン等が典型）。`def_alias`（v0.3.8・大域）が文脈依存ゆえ対象外にしていた `VAR`/`ARG` 系を、**ブロックスコープに限定すれば安全に名付けられる**——これを解禁する。実体は既存の字句マクロ＝**新opcode・新エラーコード・実行時コストすべてゼロ**。

- **`def_local(名前, 実体)` はブロック冒頭のみ・END まで局所**（`def_alias` が「ファイル冒頭・大域」なのと対をなす）。実体に書けるのは **`VAR[n]`/`SVAR[n]`（読み書き可）・`ARG[k]`/`SARG[k]`（読取専用＝受信専用）・整数/文字列リテラル（読専）**。文脈依存で大域に一意でないスロットは、局所スコープなら「このブロック内でだけこの意味」と嘘なく名付けられる。
- **役割分担（対象が排他的）**: `def_alias`＝`GVAR`/`SGVAR`/整数/文字列（大域・ファイル冒頭）／`def_local`＝`VAR`/`SVAR`/`ARG`/`SARG`/整数/文字列（局所・ブロック冒頭）。整数/文字列は双方で可。`def_local(X, GVAR[0])` のような**大域スロットは `def_local` 不可**（`ERR_SYNTAX`＝「それは `def_alias` で」）、逆に `def_alias(X, VAR[0])` も従来どおり不可。
- **利用可能ブロック**: `INIT`/`MAIN`/`ON …`/`PORT …` の**全ブロックの冒頭**（宣言は本体の最初の文より前・一括）。`ARG`/`SARG` を INIT/MAIN で別名にしても中身は空で無害。
- **衝突ルール**: (1) **別ブロック間の同名ローカルは許可**（END で捨てるので局所ライフサイクル・取り違えを誘発しない）。(2) **ローカル名が大域エイリアス名と衝突＝`ERR_SYNTAX`**（「ローカルのつもりが実は大域だった」事故を防ぐ）。(3) 予約語・スロット名・ポート名・同一ブロック内の二重宣言も `ERR_SYNTAX`（`def_alias` と同じ）。
- **実装**: `def_alias` のエイリアス表を **high-water-mark でスコープ化**するだけ——ブロック入口で表の位置を記録、冒頭 `def_local` を追記、`END` でその位置まで切り詰めて局所分を捨てる（`def_local` はトップレベルブロック冒頭のみでネストしない＝1ブロック1フラットスコープ）。展開先は既存の `LOAD_VAR`/`STORE_VAR`/`LOAD_SVAR`/`STORE_SSTR`/`LOAD_ARG`/`LOAD_SARG` に流すだけ（`ARG`/`SARG` への書き込みは既存 `ERR_BAD_POSITION`）。

例（受信プロトコルの各位置に名前を付けて読みやすく）:
```
ON RXDATA
    def_local(SRC,  SARG[0])
    def_local(DST,  ARG[1])
    def_local(DATA, SARG[2])

    SRC, " ", DST, " ", DATA -> STDOUT
END
```

**スクリプト内ポートの `VAR`/`SVAR` を private ローカル化（呼び出し元と独立）**（§3, §7）。v0.4.5 ではスクリプト内ポートは `ARG`/`SARG` だけを退避し、`VAR`/`SVAR` は呼び出し元と**共有**していた。しかしポートを「ライブラリ的」に使うと、内部で `VAR[0]` を作業に使っただけで呼び出し元の `VAR[0]` を破壊する footgun になり、致命的に使いづらい。そこで **`ARG`/`SARG` と同様に `VAR`/`SVAR` も呼び出しフレームに退避＋入口で 0/空クリア**し、戻り時に復帰する。これで **`ARG`/`SARG`/`VAR`/`SVAR` の4つが全てポートの private ローカル**になり、共有され続けるのは `GVAR`/`SGVAR`（大域）だけ＝教科書的な関数呼び出しモデルに揃う（`PORT` 内の `def_local(TMP, VAR[0])` も本当のローカル変数になる）。

- 実装は既存の `ARG`/`SARG` 退避と同じパターンの反復（`call_frame_t` に `save_var`/`save_svar` を追加・`OP_CALL_SCRIPT` で退避＋クリア・`OP_HALT` 復帰の3箇所）。新opcode/新文法なし。
- **RAM 増分**＝`CFG_CALL_NEST ×（CFG_VAR_COUNT×sizeof(value) + CFG_SVAR_COUNT×CFG_SSTR_LEN）`。int の `VAR` 退避は安価（数百B）、**`SVAR`（文字列バッファ）が支配項**（`SVAR_COUNT × SSTR_LEN × CALL_NEST`）。実際の最大呼び出し深度はロード時DFSで検算済ゆえ、**`CFG_CALL_NEST` を実深度まで絞ればRAMを直接削れる**（掛け算の係数）。文字列スロットの抜本テコ入れ（postキュー含む）と連動して効いてくる箇所。

### 変更履歴（v0.4.4 → v0.4.5）

**スクリプト内ポート定義 `PORT … END`（＝戻り値を返すサブルーチン）＋ `EXIT` を `値 -> EXIT` に統一**（§3, §4, §7, §11, §12, §13）。従来スクリプト内で書けるユーザー定義ブロックは `ON … END`（ハンドラ）だけで、これは次tick以降の**遅延評価・戻り値なし**だった。ここに**同期・値を返す**もう一系統＝**スクリプト内ポート**を足す。ユーザーから見れば要するに**サブルーチン**。ハンドラとポートの二系統が「遅延・戻り値なし・右辺専用」／「同期・戻り値あり・両辺可でチェイン可能」として対になる。

- **`PORT NAME … END`（スクリプト内ポート本体）＋ `def_port(NAME, T_INT|T_STR)`（冒頭宣言）**（§3）。`def_port` はコンパイラが解釈する3つ目の `def_`（`def_alias`／`def_handler` に続く）。宣言でポートを1本 `inout`・産出型付きで生やし、`PORT NAME … END` がその本体（バイトコード）を与える。**C実装のUtilityポート（`FORMATTER` 等）と同格の `inout` ポート**として振る舞い、`->` の**両辺に置け・チェインできる**（`1,3 -> SUM -> STDOUT`／`SUM -> VAR[0]`）。産出は既存の `RESULT`（int）／`SRESULT`（str）に乗るので**チェインも型チェックも既存機構がそのまま効く**（`1,3 -> SUM(int) -> SVAR[0]` は `ERR_TYPE_MISMATCH`）。
- **呼び出しルールの強い制約（実装を軽く保つ要）**：(1) **本体内で `WAIT` 禁止**（コンパイルエラー・`ERR_WAIT_IN_ON` を流用）＝`REPEAT` と同じ **within-tick**（呼び出しは決してtickをまたがない）。(2) **再帰禁止を静的DAG判定で一律に弾く**——スクリプト内ポート呼びは全てリテラルなポート名（動的ディスパッチ無し）ゆえ、**ロード末尾に呼び出しグラフのDFSを1回**回す。サイクルあり（自己再帰も相互再帰も同一経路）＝専用コード **`ERR_RECURSION`（`tok`＝サイクル上のポート名）**。同じDFSで**最長路＝最大コールスタック段数を算出**し、`CFG_CALL_NEST` 超過は既存 `ERR_NEST_TOO_DEEP`。実行時持ち越しは不要で、コールスタック溢れは静的に到達不能（実行時チェックは防御assertのみ）。(3) **`VAR`/`SVAR` は呼び出し元と共有**（退避しない＝サブルーチンの共有ワークエリア・私的ローカルは持たない）。**`ARG`/`SARG` はVMが自動退避/復帰する真の仮引数**（呼び出しで上書きされ、戻ると呼び出し元の値が復元される）。
- **`EXIT` を `値 -> EXIT`（値を戻すリターン文）に拡張＋単独 `EXIT` は略記として存続（後方互換）**（§7）。スクリプト内ポートでは `値 -> EXIT` で戻り値を `RESULT`/`SRESULT` に置いて呼び出し元へ返す（型は `def_port` 宣言と整合必須・`"x" -> EXIT` を `T_INT` ポートで書けば `ERR_TYPE_MISMATCH`）。ハンドラ（`ON … END`）内の `値 -> EXIT` は**値を捨てて**早期リターン。**単独 `EXIT` は「戻り型のデフォルト値を返す」略記**＝ハンドラ/`T_INT` ポートは `0 -> EXIT`、`T_STR` ポートは `"" -> EXIT` と同義（`END` 到達時のフォールスルーと完全に同一）。**`EXIT` されず `END` に達したら `0`（int）/空文字（str）を産出**。→ **既存スクリプトの裸 `EXIT`（`fib.yaj` 等）はそのまま通る＝破壊的変更なし**。
- **スクリプト内ポートを単独で左辺に置く**（`SUM -> X`・引数なしの値ソース読み）と、**引数オールゼロ・SARGオール空文字**で `PORT SUM … END` が呼ばれる（`ARG` を読む本体でも安全に0が入る）。**ポートからのハンドラ/TIMER呼び（`値 -> HANDLER`／`値 -> TIMER`）はOK**（非同期enqueue＝within-tickを壊さない・`WAIT` だけが禁止）。
- 実装インパクト: **VM初導入の同期呼び出し（戻り番地スタック）**が唯一の新概念。新opcode（`CALL_SCRIPT`〈portidx,argc〉／int戻り用 `STORE_RESULT`／`HALT` の文脈依存化 or `RET`）、**新エラーコードは `ERR_RECURSION`（サイクル・load時）1つのみ**（深度超過=`ERR_NEST_TOO_DEEP` を再利用）、新定数 `CFG_CALL_NEST`、port_t に `bc_start`、vm にコールフレーム配列（`ret_pc`＋`ARG`/`SARG` 退避）、コンパイラに**ロード末尾の呼び出しグラフDFS（サイクル検出＋最長路＝最大段数の静的検算）**。制約(1)(2)(3)により VAR/SVAR 退避もクロスtick永続化も実行時再帰チェックも不要で、負荷は v0.4.4 と同等〜やや上。

### 変更履歴（v0.4.3 → v0.4.4）

**有界ループ `REPEAT` ／ 反復カウンタ `ITR` ／ スロットの動的添字アクセス `N -> SLOT` の3点セット**（§3, §4, §7, §12, §13）。Yajir 初期の割り切り「ループ無し・添字は定数のみ」を、実用スクリプトが配列反復で壁に当たったため2つとも解く。安全性は既存の命令数バジェットと benign-0 縮退でほぼ据え置き（後方互換）。

- **`N -> REPEAT … END`（有界ループ）＋ `ITR`（反復カウンタ 1..N）**（§3, §7）。`<N> -> REPEAT` で本体を N 回実行。`ITR` は `REPEAT … END` 内だけで有効な読み取り専用の値源で、現在の反復回数 **1..N（1始まり）** を返す。N は**入口でスナップショット**（ループ中に元を変えても効かない）、`N ≤ 0` は0回。ネストは `CFG_LOOP_NEST=4` 段まで・`ITR` は**最内**を指す。ループ状態（カウンタ/上限/ITR）は vm のループフレームに持つ（オペランドスタックではない）。
  - **`REPEAT` 内で `WAIT` はコンパイルエラー（新 `ERR_WAIT_IN_LOOP`）**。これが設計の要：WAIT を禁じることで **`REPEAT` は「1 tick 内で完結し決してティックをまたがない within-tick 構造」**になり、ループ状態を yield またぎで永続化する必要も、MAIN の resume 時 sp リセットで壊れる問題も生じない。検出はコンパイラの `repeat_depth>0` フラグ1個（既存 `yield_ok` の隣）。**`TIMER` は許可**（yield せず「予約して継続」なので within-tick を壊さない）。
  - **暴走ガード＝バジェット**: 後退辺も1命令ずつ `CFG_INSTR_BUDGET` を消費し、超過時は **`END` の直後へ pc を飛ばして打ち切り**＋`STATUS` に新ビット **`ERR_BUDGET`**（中断点が常に文境界＝MAIN の resume も破綻しない・「途中で黙って止まった」を観測可能に）。挙動要約＝「**完走 or その場で打ち切り、いずれも同一tick内で決着**」。
  - **役割の線引き**: `REPEAT` は**同期・within-tick の計算用**（時間は進まない）。**時間を進めるペース配分は MAIN＋WAIT／`ON <周期>`／self-post 反復**で書く。深い/重い反復でタイミングを乱したくないなら self-post（1歩/tick・tick を専有しない）。深い `REPEAT` はその tick を最大バジェット分伸ばす＝ジッタ注意（クラッシュはしない）。
- **スロットの動的添字アクセス `N -> SLOT`（read）／`idx, val -> SLOT`（write）**（§4）。従来「添字は定数リテラルのみ」を、**スロット名を裸のポートとして使うと動的添字アクセスになる**形で解禁。**第1引数は常に添字（1始まり）**：`N -> VAR`（1引数）＝リード `VAR[N-1]` を産出／`idx, val -> VAR`（2引数）＝ライト `VAR[idx-1]=val` し**書いた値を産出**。**産出型はスロットの要素型**（int→`RESULT`／str→`SRESULT`）＝そのままチェインに乗る（`ITR -> VAR -> STDOUT` 走査・`ITR, 式 -> VAR` 充填）。**範囲外は benign**（リード=0/空文字・ライト=no-op＋0）でメモリ安全は実行時境界チェックが担保。**書く値の型は静的チェック維持**（`idx, "x" -> VAR` は `ERR_TYPE_MISMATCH`）。**`ARG`/`SARG` はリードのみ・ライトは `ERR_BAD_POSITION`**（受信専用の構造違反はコンパイル時に弾く）。**始点は1**（`ITR` の 1..N と整合。既存 `SLOT[k]` の0始まりとは別系統・docs明記）。
- 実装インパクト: 新opcode（`REPEAT_INIT`/`REPEAT_NEXT`/`LOAD_ITR`/`SLOT_INDEX`〈kind,argc〉程度）、新エラー `ERR_WAIT_IN_LOOP`（ロード時）・`ERR_BUDGET`（STATUS 実行時ビット）、新定数 `CFG_LOOP_NEST=4`、vm 構造体にループフレーム配列。言語の他部分・既存スクリプトへの影響なし。

### 変更履歴（v0.4.2 → v0.4.3）

**数値ユーティリティ・ポート群を標準装備に追加（`RAND`/`SEED`・`CLAMP`・`MAP`・`MIN`/`MAX`・`ABS`）＝ 文字列ユーティリティの数値版**（§3）。「変換はポート」に則り int産出のポートとして提供（`register_mathutils()`＝`register_strutils` と同経路）。

- **`RAND`（in・非負乱数源 0..2³¹-1）＋ `SEED`（out・再シード）**: 実体は libc `rand()` 非移譲の自前 **xorshift32**（状態1ワードをアリーナ内 `rng_state` に置く・再現的・full 32bit・状態0は既定種へ自動退避）。範囲は `RAND % N` で作る。符号ビットを落とし非負固定（`RAND % N` が負にならない）。`値 -> SEED` で再シード、ホストは `script_srand(entropy)` で起動時に種注入。※暗号用途ではない。
- **`CLAMP`（x,lo,hi）** 範囲飽和 ／ **`MAP`（x,inLo,inHi,outLo,outHi）** Arduino `map()` 相当の線形リスケール（中間積は int64 でオーバーフロー回避・`inHi==inLo` は `RESULT=0`＋`ERR_DIVZERO`・レンジ外は外挿）。
- **`MIN`/`MAX`（可変長）・`ABS`**（`ABS(INT32_MIN)`→`INT32_MAX` 飽和）。
- 付随: `CFG_MAX_PORTS 48→56`（組込み〜29本）・`SCRIPT_VERSION "0.4.3"`・サンプル `scripts/mathdemo.yaj`・テスト `test_phase10_math.c`。

### 変更履歴（v0.4.1 → v0.4.2）

**値の型を int / str の2択に純化する（内部 CHAR タグを撤去）＋ 文字リテラル `'A'` ＋ 変換Utility `CHR` の3点セット**（§2, §3, §5, §9, §10, §11）。

- **内部 `CHAR` 型を撤去し、値は int か str の2択に純化**（§9）。従来 VM 値は `int / char / str` の3タグ（`char`＝「.i に文字コードを持つ整数で、STDOUT はグリフ表示する」）だったが、`char` は**型というより「出力時のレンダリング指示が値に相乗りしているだけ」**で、実効は STDOUT の1分岐のみ。しかも `GVAR`（int スロット）を1回通るとタグは落ちる＝**直の `ARG[0] -> STDOUT` 一発しか生きない脆いタグ**で、実質「型」ではなかった。これを撤去し「**値は数か文字列。数を文字グリフとして描くかは出力側の仕事**」に整理する。撤去対象: 値タグ `SV_CHAR`／`val_char`／host STDOUT の char 分岐／`script_post_msg_char`・`SCRIPT_ARG_CHAR`（int 版と同義になるので薄い別名で残すか撤去）。UART RX 1バイトは**ただの int** として `ARG[0]` に入る。
- **文字リテラル `'A'` を字句として追加（＝整数リテラルの別表記）**（§2）。`0x41` や `65` と完全に同格の「整数の綴り違い」。`'A'` は int 値 65 を積むだけで、専用タグは持たない。ゆえに `'A' -> GVAR[0]`（int スロット）はOK、`(ARG[0] == 'A')` は素直な int 比較（`== 65` より可読）、`'A' + 1` は int 算術。`'A' -> SVAR[0]`（str スロット）は型違いで `ERR_TYPE_MISMATCH`。エスケープは文字列と同じ `'\r' '\n' '\t' '\\' '\''`。空 `''`・複数文字 `'AB'` は `ERR_SYNTAX`。実装は「トークナイザの字句＋push（int値として）＋ parse_primary の1分岐」で、新しい値タグや型は増えない。
- **変換Utility `CHR`（int→1文字str）を標準装備に追加**（§3）。CHAR タグ撤去で「数を文字グリフで出す」置き場が要るが、これは**出力側のポート**に持たせる＝ Yajir の「変換はポート」（`STRTOL`＝str→int の対）に一致。`65 -> CHR` → `SRESULT`=`"A"`。UART エコーは `ARG[0] -> CHR -> STDOUT` と書く（`FORMATTER "%c"` の一引数糖衣に相当。`FORMATTER` の `%c` は従来どおり int を受けてグリフ化するので温存）。
- **帰結（後方互換の注意）**: 旧イディオム `ARG[0] -> STDOUT`（受信文字のエコー）は、グリフではなく**10進数**を出すようになる。エコーは `ARG[0] -> CHR -> STDOUT`（または `"%c", ARG[0] -> FORMATTER -> STDOUT`）へ書き換える。1.0前ゆえ許容とするが破壊的変更として明記。得るもの＝型モデルが int/str の2択に純化・`'A'` が「整数の別表記」として一貫・変換が全部ポートに乗る世界観の統一。

**制御語 `EXIT` を追加（ON ハンドラの早期リターン）**（§3, §7, §13）。`ON … END` 内で、エラー条件等により**そのハンドラを途中で即終了**したいときに書く裸の制御語（`END`/`ELSE` と同じ制御語カテゴリ・`-> 送り先` を伴わない）。ネストした `IFYES` の内側からでもハンドラ**全体**を抜ける（C の `return` 相当・`break` のような一段だけの脱出ではない）。**`INIT`/`MAIN` 内では構文エラー**（`ERR_SYNTAX`）＝ON専用（`WAIT` の「ON不可」判定のちょうど逆で、`yield_ok` フラグで静的判別）。実装は**新opcode不要**——「その場で既存の `OP_HALT`（ブロック終端＝`EXEC_DONE`）を吐くだけ」で、ON ハンドラは yield せず走り切る（resume状態を持たない）ので早期HALTが安全に効く。コンパイラの `parse_stmt` 冒頭で横取り＋予約語1語追加のみ。

### 変更履歴（v0.4 → v0.4.1）

- **ISR から `script_post_msg*` を直接呼べるよう正式化＝イベントキューを MPSC 安全化**（§10, §11, §12）。従来「post は ISR安全（積んで即return）」と述べつつ、実体のSPSCリングは**単一生産者前提**だった。Yajir はスクリプト自身が self-post でき（`値 -> ハンドラ源`・`INVOKER`＝メイン文脈の生産者）、加えて生ISRからも post したい（ボタンEXTI／UART RX 等）ため、生産者が複数になり `evq_tail` 更新が競合しうる（低頻度ゆえ実害は出にくいが潜在レース）。**enqueue（`evq_push` のtail公開）を短いクリティカルセクションで囲い MPSC 安全にする**ことで、「任意のISRから直接 post してよい」を保証へ格上げ。**disable_irq 相当はプラットフォーム依存**なので、コアは `YJ_ENTER_CRITICAL()` / `YJ_EXIT_CRITICAL()` の2マクロ越しに囲み、**コアは既定で空定義（純C・ホスト非依存を維持）**・各ホストが自ターゲットの割り込み禁止/復帰へ割り当てる（STM32=CMSIS の `PRIMASK` 退避 `__get_PRIMASK()`/`__disable_irq()`/`__set_PRIMASK()`）。単一生産者しか無いホストは空マクロのままでよい（コスト厳密ゼロ）。dequeue（消費）はメインループ1本のままで不変。新opcode無し・言語文法変更無し。ホストサンプルの「ISRではフラグだけ立て、実postはメインループ」という回避策は本対応で不要になる。
- **`def_handler(NAME)` をスクリプト側宣言として正式採用**（§3, §10, §13）。従来ハンドラ源は C側 `script_register_handler` が唯一の登録経路で、スクリプトの `def_handler` 行は非解釈のドキュメントだった。**handler はポート種別で唯一「束縛すべきCの実体（関数/値）を持たない＝名前だけのイベントチャネル」**なので、`def_alias`（v0.3.8）に続き**コンパイラが実際に解釈する2つ目の `def_`** に格上げする：`def_handler(NAME)` はコンパイル時にハンドラ源ポートを1本 `add_port` し、以後 `ON NAME` と `値 -> NAME`（self-post）が普通に解決される。これで **Cを1行も書かず、スクリプト内だけで完結する名前付きハンドラ／状態機械**（`INVOKER` 動的発火・self-post 反復と併用）が書ける。意味は**「このハンドラ源の存在を保証する（冪等）」**——C側が既に同名を register 済みなら no-op（同種再束縛）、無ければ新規に生やす。ゆえに既存スクリプトの `def_handler(UART1)`（Cが登録済み）等は**挙動不変で後方互換**。`def_in`/`def_out`/`def_inout` は**C関数ポインタが必須**ゆえスクリプト側では作れない（引き続き非解釈ドキュメント）＝「Cが要るものはC側、名前だけで済む handler はスクリプト側」の線引き。構文は既存 `def_*`／`def_alias` に合わせ **bare identifier**（`def_handler(RETRY)`・クォート無し）、宣言は使用より前（`def_alias` と同様に冒頭一括）。**衝突**（予約語/スロット名/既登録の非handlerポートと同名）は `ERR_SYNTAX`、**ポート表満杯**は新設 `ERR_TOO_MANY_PORTS`（従来の add_port 満杯時「黙ってNULL＝無言で登録失敗」を、スクリプト経路では load エラーへ格上げ＝実装で踏んだ無言オーバーフローの再発防止）。

### 変更履歴（v0.3.8 → v0.4）

- **パイプライン・チェイン `A -> F1 -> F2 -> 終端` を確定・正式採用**（§5, §13, §14, §15）。§16 で「将来の楽しみ」として寝かせていた本機能を、v0.3.8 の**ポート属性システム（向き×産出型）を土台に**、**案ア（純パイプ）で確定**。1行に `->` を複数連ね、前段の産出値（`RESULT`/`SRESULT`）を次段へ**そのまま**流すパイプライン。
- **実行モデルは不変＝複数文への純粋なコンパイル時糖衣（desugaring）**。`A -> F1 -> F2` を `A -> F1` / `<F1の産出> -> F2` に展開してから**いつものバイトコードを吐く**。`->` は値を返す演算子に**ならない**（ポートは終端のまま・「1文1射」がN個並ぶのと完全同一）。**新opcode・新エラーコードはゼロ**（位置違反＝既存 `ERR_BAD_POSITION`、終端スロットの型違い＝既存 `ERR_TYPE_MISMATCH`）。`def_alias`（v0.3.8）と同じく「足さずに済む」を維持。
- **継ぎ目の型は産出型属性が決める**（§3）。各継ぎ目で int産出なら `RESULT`、str産出なら `SRESULT` を**暗黙の左辺**として挿す。**継ぎ目では型チェックしない**（受け側ルーズ・入力期待型を持たない）＝型が合わなくても benign 値（`0`/空文字, §4・§5）で受けるだけで破綻しない。鎖の途中で int↔str が変わってよい。型チェックが入るのは**終端がスロット**（`GVAR`/`SGVAR`…）のときだけ（既存の型分離）。
- **位置規則**（属性表を引くだけ・load時静的・実行時ゼロ）: 先頭＝`expr_list`（多値可）／中間＝`inout` のみ／終端＝`out`/`inout`/`handler`/制御(`IFYES`/`WAIT`/`TIMER`)/書込可スロット。`out`/`handler` は産出noneゆえ**中間不可・終端のみ**。`in` は右辺不可（先頭のみ）。違反は `ERR_BAD_POSITION`。
- **加工・合流は鎖の中では不可**（案ア確定）。`RESULT+1` の加工も `, 他値` の合流も禁止＝継ぎ目は常に**単一値を通す管**で分岐しない。必要なら鎖を切って複数文で書く。**帰結: 多引数は先頭段だけ**——継ぎ目以降は常に1値ゆえ、`FORMATTER`/`SLICER`/`EQUALS` 等の多引数Utilityは**鎖の先頭段**（先頭の `expr_list` から全引数をもらう）としてのみ機能（下流段に置くと1引数のみ・残り0埋め）。ここは救わない。
- 設計経緯（実装の参考）: 願望「パイプ＝関数合成 `F2(F1(A))`」を、Yajir の芯「ポートは値を返さない終端」を壊さず実現する解。`->` を式演算子化せず、属性システムの上にコンパイル時糖衣として乗せた。案ア+（前段結果の単項加工 `RESULT*2` を許す案）も検討したが、実コードの手癖（`FORMATTER -> WISUN -> IFYES` が各継ぎ目素通し）が加工を要求しなかったため**純パイプに確定**。§16 の該当項目を「採用済み」へ格上げ。

### 変更履歴（v0.3.7 → v0.3.8）

- **ポート属性システムを確定**（§3, §11）。各ポートに「**向き**（in=左辺のみ／out=右辺のみ／inout=両辺OK）」と「**産出型**（int/str）」の2属性を持たせる。属性は `def_*` テキストではなく `script_register_*` 呼び出しが運ぶ真実で、コンパイラが `->` の**位置チェック**と格納先の**型チェック**に使う。
- **産出型は out 以外で必須・明示**（§3, §11）。`def_in`/`def_inout` は登録時に産出型（`SCRIPT_T_INT`/`SCRIPT_T_STR`）を必ず渡す。`def_out` は**産出none確定**ゆえ指定そのものが存在しない（enumを受け取らない）。書き忘れ＝Cビルドが通らず、産出型のすり抜けを根絶。
- **戻り値を持つ関数ポートは out → inout へ再分類**（§3）。`RESULT`/`SRESULT` を産む的（`FORMATTER`/`SLICER`/`WISUN`/計算系）は「産出none」では表せないため `def_inout`＋産出型。値を返さない的（`LED1`/`BUZZER`/`STDOUT`）は `def_out` のまま。
- **ルーズ引数は不変**（§5）。右辺で値を受ける側の左辺は従来どおり可変長・型自由・並び自由。産出型は**産出する側**（値が出る向き）にのみ用い、格納先・将来のチェイン継ぎ目のチェックに使う。
- 新エラーコード **`ERR_BAD_POSITION`**（§11）: 向き違反（`1 -> SENSOR`／`LED1 -> VAR[0]` 等）。産出型違反（`READER`(str)`-> VAR[0]`(int)）は既存 `ERR_TYPE_MISMATCH` を再利用。
- 本属性は**チェイン（`A->F1->F2`）の土台**だが、チェイン構文自体はまだ入れない（§16・別途）。属性単体でも `->` の位置/型を静的に弾けて有用。
- **`HANDLER` の特殊扱いを撤廃・ハンドラ源を向き軸に正式組み込み**（§3, §6, §10）。`値 -> ハンドラ源` を正式許可（旧 `ERR_SYNTAX` 廃止）＝全ハンドラ源がスクリプトの `->` 右辺に立てて self-post できる。`HANDLER` は**起動時に自動 `script_register_handler` される既定のハンドラ源**へ格下げ（専用opコード・専用ディスパッチ分岐を廃止・ユーザー定義のハンドラ源と内部完全同格）。`def_handler` の向き＝「右辺可(post先)・受けて即post・産出none」。チェイン的にはハンドラ源は**産出none ゆえ終端のみ**（中間不可・`ERR_BAD_POSITION`）。「内蔵 self-post 源 `HANDLER` は唯一・特殊」という記述を「全ハンドラ源が self-post 可能・`HANDLER` はその既定インスタンス」へ一般化。
- **用語・API実名を `irq` → `handler` に一本化**（全§）。旧 `irq`／「割り込み源」は実態（割り込み起源とは限らず・タイマ満期/周期/C post/self-post も同じ受け口 `ON … END`）と乖離し誤解を招くため廃止。マクロ `def_irq`→`def_handler`、C-API `script_register_irq`→`script_register_handler`、向き軸ラベル `irq`→`handler`。発火源で「タイマハンドラ（`ON TIMER`）／周期ハンドラ（`ON <ms>`・無名）／ユーザー定義ハンドラ（`ON UART1`/`RXDATA`/`MYHANDLER`）／既定ハンドラ（`ON HANDLER`・1個）」と呼び分ける。**物理ISR・割り込み起源 post を指す箇所の「割り込み」は本来の意味なので温存**。

- **別名 `def_alias` を追加**（§3, §13）。`def_alias(名前, 実体)`＝`#define` 相当の純粋な字句マクロ（名前→実体トークン列に展開してから通常処理・実行時コスト厳密ゼロ・新opcode/新エラーコード無し）。実体は3種: (A)スロット `GVAR[n]`/`SGVAR[n]`（読み書き可・型はスロット継承）、(B)文字列リテラル（読専str）、(C)整数リテラル（読専int）。`VAR`/`ARG` は文脈依存ゆえ対象外（原則: 別名対象は実体が大域に一意なもの）。別名は属性を持たず**展開後の対象がすべてを決める**（read-only等の専用判定不要）。**宣言はスクリプト冒頭に一括（義務）**。`def_const` は不変＝システム提供の定数用、`def_alias` はユーザー定義（一連の事情を知らない書き手が能動的に書く `def_*` は実質これだけ）。

### 変更履歴（v0.3.6 → v0.3.7）

- **ロードエラーを構造化**（§3, §11）。`script_load` 失敗時、`script_last_error()` が `script_error_t {int16 line, script_err_t code, int16 aux, char tok[]}` を返す。理由を enum で持ち、**文字列化はコアでは行わない**（(B)方針＝組み込む人が表示・言語・did-you-meanを担当）。`script_strerror` は任意リンクのデバッグ補助。
- **エラーコード列を定義**（§11）: `ERR_END_EXPECTED`(aux=開きヘッダ行)/`ERR_UNEXPECTED_END`/`ERR_UNKNOWN_PORT`(tok)/`ERR_UNKNOWN_NAME`(tok)/`ERR_TYPE_MISMATCH`/`ERR_WAIT_IN_ON`/`ERR_BAD_SLOT_INDEX`/`ERR_NEST_TOO_DEEP`/`ERR_SYNTAX`。ワンパスで**最初の1個で停止**（回復処理は持たない）。
- **ロード時エラーと実行時STATUS異常は別系統と明記**（§11, §12）。ロード時＝`script_err_t`（入口の門前払い・一度きり・`script_load`戻り値）／実行時＝`STATUS`ビット（走行中の観測可能フラグ）。名前空間が `ERR_` で被るが別物。
- **`script_tick` の処理単位＝1世代（generation）を確定**（§6, §10）。tick開始時点のキュー内容（スナップショット）をそのtickで全件ディスパッチし、drain中に積まれた子は次tickへ繰り越す。self-post は**幅優先（BFS）**で世代＝tick境界に展開。同一ハンドラ内の複数 self-post は**文の出現順**でenqueue。ツリー展開はキュー長で制限され、溢れた枝は**枯れる**（実行されず・`queue_overflow` フラグ）。実装は現状どおりで保証への格上げ。
- `tok`（問題トークン断片）長は §12 の識別子最大長を流用（新定数なし）。did-you-mean 等の照合はコア非搭載（生データのみ提供）。

### 変更履歴（v0.3.5 → v0.3.6）

- **内蔵 self-post 源 `HANDLER` を仕様に正式記載**（§3, §6, §10）。`def_handler`/`script_register_handler` 不要で最初から `ON HANDLER` が使える唯一の内蔵イベント源。受け口は `ON RX` と同型（`ARG`/`SARG` 位置共有ビュー・多値int/str混在可）。実装には既に存在、仕様を追従させる差分。（用語: 旧 `def_irq` を v0.3.8 で `def_handler` に改称）
- **`式リスト -> HANDLER` は即時1発post＝非再帰を明記**（§6, §10）。その行で本体へ降りない（`TIMER` 張りと同じ「予約して即return」）。次tick以降のディスパッチで `ON HANDLER` が回る。スタックを積まず深さ無制限で安全。
- **self-post 反復を正式イディオムに**（§6）。配列・可変添字を持たない Yajir での反復の正準形（「Nステップかけて状態機械を1段ずつ進める」）。`for`/`REPEAT` は不採用のまま。
- 将来 `HANDLER[i]` 複数化を §16 に記載（Cを書かないユーザーの「定義済みハンドラ」代替経路）。

### 変更履歴（v0.3.4 → v0.3.5）

- **受信文字列引数スロット `SARG[n]` を追加**（§4, §10）。`ARG[k]` と `SARG[k]` は同じ位置 k を**型で振り分けて読む2ビュー**（別カウンタではない・位置共有）。型不一致/不足は benign 値（`0`/空文字）を返す。受信専用。
- **マルチ引数post `script_post_msg_v` を追加**（§11）。混在・複数引数（例 RX: src=int, dst=int, data=str）を**1イベントとしてアトミックに**積む。str引数はpost時に SARG バッファへコピー（超過は切り詰め＋`ERR_STR_TRUNC`、新フラグ無し）。型 `script_arg_t`／組立マクロ `SCRIPT_ARG_INT/CHAR/STR`（接頭辞は `script_*`/`SCRIPT_*` で統一）。
- **キューRAMは一律固定エントリに確定**（§12）。`queue ≈ Q×(N_ARG×4 + N_SARG×SARG_LEN + α)`。全エントリ同サイズ・静的見積もり。プール方式は将来ノブ（§16）。
- 付随: §13 EBNF に文字列スロット（`S*`／従来欠落）と `SARG` を明記。既存の単値post 2本は据え置き。

### 変更履歴（v0.3.3 → v0.3.4）

- **INIT を2フェーズ・ライフサイクルとして再定義**（§1, §6, §7, §8, §10）。起動は INITフェーズ（世界は凍結）→ RUNフェーズ（始動）の2段。INIT脱出の瞬間を t0 とし、周期 `ON` の初回・INITで張った `TIMER` の締切を t0 起点に解決する。
- **`WAIT` を INIT でも解禁**（§7）。yield可能ブロックを INIT/MAIN に拡張（`ON` は引き続き禁止）。中断スロットは MAIN と共用（実行時メモリ増ゼロ）。
- **INITフェーズ中はイベントの橋を閉じる**（§10）。割り込み起源含む全 post を INIT中は no-op で破棄（**保証**・フラグも立てない）。INIT→RUN 切替時にキューを空リセット。
- 付随確定: 長い INIT WAIT は t0 起点ゆえ catch-up債務を生まない（§6）。INIT に `ARG[]` は無し（0扱い）。再ロードは INITフェーズに入り直す。

### 変更履歴（v0.3.3 → 命名確定）

- **言語名を `Yajir`（ヤジール）に確定**。拡張子 `.yaj`。由来は `->`（矢印／野次る）。
- C側API接頭辞は当面 `script_*` のまま据え置き（実装進行中のため、リネームは見送り）。

### 変更履歴（v0.3.2 → v0.3.3）

- **`ON <条件>` を `ON <周期> (条件)` に整理**（§6, §16）。インターバル必須・条件は副作用なしの純粋式に限る、を確定。レベル/エッジの別とフラグのクリア作法は実使用まで保留（決定に必要なのは使用感のみ、という所まで分解済み）。

### 変更履歴（v0.3.1 → v0.3.2）

- **`def_*` の位置づけ**を確定（§3）。実体はC側 `script_register_*` が唯一の真実、`def_*` 行は人間向けドキュメント（解釈しない）。未登録の名前使用は `script_load` 時にコンパイルエラー（タイポ検出）。
- **v0.3 全機能ショーケースサンプル**を §14 に追加（正式採用・回帰基準）。PC上のリファレンス実装で出力確認済み。

### 変更履歴（v0.3 → v0.3.1）

- **文字列スロットを `->` の両辺に**置けることを確定（§4）。リテラル代入 `"x" -> SVAR[0]`、スロット間コピー `SVAR[0] -> SVAR[1]`（実質strcpy）、読み出し `SVAR[0] -> COUNTER`、結果保存 `SRESULT -> SGVAR[0]` を単一の代入処理で実現。
- **型の分離**を明文化（§4）。intスロットと文字列スロットの相互代入はコンパイルエラー（スロット名で判別・実行時コストゼロ）。

### 変更履歴（v0.2 → v0.3）

- **演算拡張**: 乗除剰余 `* / %` と**ビット演算** `& | ^ ~ << >>` を追加（§9）。実装はopcode＋優先順位行のみ。引数位置の各要素にも式が書ける（連動）。
- **`/0` `%0` の扱い**: 結果0で**継続**。異常は `STATUS` の `ERR_DIVZERO` ビットで記録（§9, §12）。
- **数値リテラル拡張**: 16進 `0xFFFE`・2進 `0b1010`（大小文字可、`_` 桁区切り可）を追加（§2）。
- **`STATUS` 入力ポート ＋ `CLEAR_ERR` 制御ポート**: 各種異常フラグ（キュー溢れ/タイマ満杯/0除算/文字列切詰）を `STATUS` に集約。`(STATUS & ERR_xxx) -> IFYES` で判定、明示クリア（§3, §12）。
- **文字列処理Utility（標準装備）**: `SLICER / MERGER / COUNTER / FORMATTER / EQUALS` をポートとして提供。言語コア（文法・VM）は不拡張（§3 標準装備Utility）。
- **固定長文字列スロット（案A）**: `SVAR[] / SGVAR[] / SRESULT` を追加。ヒープ不使用、int版スロットと対・同生存規則（§4 文字列スロット）。
- 文字列の流儀: **位置は1始まり・範囲は(開始, 長さ)**、**1バイト1文字・ASCII前提**（マルチバイトは将来）。

### 変更履歴（v0.1 → v0.2）

- 同一tickで複数の周期ハンドラが満期したときの順序を**定義順（ソース出現順）**に確定（§6）。
- 遅延で周期満期が複数回パスされたときの**満期ポリシー**を確定: 既定=**catch-up**、コンパイルオプションで**coalesce**に切替（§6）。catch-up時の取り戻しは以降のtickに1回ずつ分散（同tickバースト回避）。
- `STDOUT` 等の**可変arityポート**を明文化（供給全引数を順に処理。過多切り捨ての例外）（§5）。
- PC上のリファレンス実装で §14 サンプルの動作を確認済み。catch-up/coalesce 検証サンプルを §14 に追加（正式採用）。

### 変更履歴（v0 → v0.1）

- 複数引数の送信 `A, B -> PORT` を追加。供給過多は切り捨て・供給不足は0埋め（全ポート共通）。
- ユーザー定義可変変数（`def_var`）を**廃止**。固定スロット `GVAR[] / VAR[] / ARG[] / RESULT` に一本化。
- `RESULT`（直前の関数ポート送信の戻り値）を追加。更新規則を明文化。
- `VAR` は WAIT またぎ非保証。保持は `GVAR`。を確定。
- `WAIT` は **MAINブロック専用**。INIT/ON 内は**コンパイルエラー**。を確定。
- `TIMER`（ワンショット・自己再アームで周期化）を**確定**（将来ノブから格上げ）。
- パイプライン・チェイン `A -> F1 -> F2` は**コスト見積もり済み・未着手**（一直線限定なら安価。§16）。
